<div class="container-fluid">
  <div class="row">
    <div class="col-sm-6">
      <h3>Demo</h3>
      {{!- BEGIN-SNIPPET horizontal-infinite-scroll-example }}
        <div class="table-wrapper dark scrollable-horizontal">
          {{#horizontal-collection model.numbers
            estimateWidth=90
            staticWidth=true
            bufferSize=5

            firstReached="loadBefore"
            lastReached="loadAfter"

            as |item index|
          }}
            {{number-slide item=item index=index}}
          {{/horizontal-collection}}
        </div>
      {{!- END-SNIPPET }}
    </div>
    <div class="col-sm-6">
      <h3>Infinite Scroll</h3>
      <p>
        The Horizontal Collection can be used to quickly build a robust infinite scroll.
      </p>
      <p>
        Your infinite scroll can be bi-directional, loading new content above or
        below.
      </p>
      <p>
        Combined with `idForFirstItem`, this makes it very easy to load a user
        in-media-res: ideal for caching their position in a long feed for when
        they return.
      </p>
      <h4>Code for Demo</h4>
      {{code-snippet name="horizontal-infinite-scroll-example.hbs"}}
    </div>
  </div>
</div>
